# FindLy Engine Requirements
# Core dependencies for file indexing and vector search

# Vector search and embeddings
faiss-cpu>=1.7.4          # CPU version of FAISS for vector similarity search
numpy>=1.24.0             # Numerical operations

# File system monitoring
watchdog>=3.0.0           # File system event monitoring

# Database
# sqlite3 is built-in with Python, no need to install

# PDF text extraction (required for real PDF chunking)
pdfplumber>=0.10.0

# Text embeddings (optional but recommended for semantic search; backend enables by default)
sentence-transformers>=2.2.0

# pillow>=9.0.0                 # Image processing (optional)

# Development and testing
pytest>=7.4.0             # Testing framework
pytest-cov>=4.1.0         # Test coverage
